import { CodeartifactClientConfig } from "@aws-sdk/client-codeartifact";

// FIXME: populate all required configuration from environment variables
const configuration: CodeartifactClientConfig = { region: "us-west-2" };

export default configuration;
